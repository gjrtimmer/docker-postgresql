image: registry.timmertech.nl/docker/docker:latest

stages:
  - build
  - trigger

variables:
  CI: "true"
  DOCKER_DRIVER: overlay2

################################################################################
# Workflow
workflow:
  rules:
    # If $VERSION variable defined have it take precedence over the commit tag
    - if: $VERSION
      variables:
        RELEASE: "${VERSION}"
    # When the CI_COMMIT_TAG is present we are override the Gradle command
    # and configuring the tag as the version to build
    - if: $CI_COMMIT_TAG
      variables:
        RELEASE: "${CI_COMMIT_TAG}"
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: "$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS"
      when: never
    - if: "$CI_COMMIT_BRANCH"
    - when: always

################################################################################
# Template(s)
################################################################################
.build:
  stage: build
  interruptible: true
  variables:
    DOCKER_IMAGE: ${CI_REGISTRY}/${CI_PROJECT_PATH}:latest
  before_script:
    - echo "${CI_DEPENDENCY_PROXY_PASSWORD}" | docker login -u "${CI_DEPENDENCY_PROXY_USER}" --password-stdin "${CI_DEPENDENCY_PROXY_SERVER}"
    - echo ${CI_JOB_TOKEN} | docker login -u gitlab-ci-token --password-stdin ${CI_REGISTRY}
    - echo ${DOCKERHUB_PASS} | docker login -u ${DOCKERHUB_USER} --password-stdin ${DOCKERHUB_REGISTRY}
  script:
    - make build push

################################################################################
# Job Configuration
################################################################################
psql:13:
  extends: .build
  variables:
    ALPINE_VERSION: "3.15"
    DOCKER_FILE: Dockerfile.13
    DOCKER_IMAGE: ${CI_REGISTRY}/${CI_PROJECT_PATH}:13
    DOCKER_IMAGE_HUB: ${DOCKERHUB_REGISTRY}/${DOCKERHUB_NAMESPACE}/${CI_PROJECT_PATH}:13
  only:
    - master
    - schedules
  after_script:
    - docker tag ${DOCKER_IMAGE} ${DOCKER_IMAGE_HUB}
    - docker push ${DOCKER_IMAGE_HUB}

psql:14:
  extends: .build
  variables:
    ALPINE_VERSION: "3.15"
    DOCKER_FILE: Dockerfile.14
    DOCKER_IMAGE: ${CI_REGISTRY}/${CI_PROJECT_PATH}:14
    DOCKER_IMAGE_HUB: ${DOCKERHUB_REGISTRY}/${DOCKERHUB_NAMESPACE}/${CI_PROJECT_PATH}:14
  only:
    - master
    - schedules
  after_script:
    - docker tag ${DOCKER_IMAGE} ${DOCKER_IMAGE_HUB}
    - docker push ${DOCKER_IMAGE_HUB}

psql:latest:
  extends: .build
  variables:
    ALPINE_VERSION: "3.15"
    DOCKER_FILE: Dockerfile
    DOCKER_IMAGE: ${CI_REGISTRY}/${CI_PROJECT_PATH}:latest
    DOCKER_IMAGE_HUB: ${DOCKERHUB_REGISTRY}/${DOCKERHUB_NAMESPACE}/${CI_PROJECT_PATH}:latest
  only:
    - master
    - schedules
  after_script:
    - docker tag ${DOCKER_IMAGE} ${DOCKER_IMAGE_HUB}
    - docker push ${DOCKER_IMAGE_HUB}

branches:
  extends: .build
  variables:
    DOCKER_IMAGE: ${CI_REGISTRY}/${CI_PROJECT_PATH}:${CI_COMMIT_REF_SLUG}
  only:
    - branches
  except:
    - master
# EOF

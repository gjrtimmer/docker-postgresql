#!/usr/bin/with-contenv bash
# shellcheck shell=bash disable=SC2034

# Variables
TZ=${TZ:-UTC}

# This will redirect all logging to stdout/stderr
# Intended to be used for NOMAD / Kubernetes
#
# When on it will redirect all logging to stdout/stderr
# With the exception of the upgrade logs
LOG_REDIRECT=${LOG_REDIRECT:-off}
